interface:change_transaction
    op
        commit
    negative
        yes

interface:change_transaction
    op
        rollback
    negative
        yes
/*
interface:destroy
    ifacename
        #IFACENAME#

interface:iface_mac

interface:undefine
    ifacename
        #IFACENAME#

interface:iface_mac

interface:change_transaction
    op
        begin

interface:change_transaction
    op
        begin
    negative
        yes

interface:define
    ifacename
        #IFACENAME#
    ifacetype
        #IFACETYPE#

interface:change_transaction
    op
        rollback

interface:change_transaction
    op
        begin

interface:define
    ifacename
        #IFACENAME#
    ifacetype
        #IFACETYPE#

interface:change_transaction
    op
        commit

interface:iface_mac

interface:create
    ifacename
        #IFACENAME#

interface:iface_list
    flags
        all

interface:iface_list
    flags
        active

interface:iface_list
    flags
        inactive
*/
