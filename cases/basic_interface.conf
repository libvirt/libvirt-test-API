interface:destroy
    ifacename
        $testnic

interface:iface_mac

interface:undefine
    ifacename
        $testnic

interface:iface_mac

interface:define
    ifacename
        $testnic
    ifacetype
        ethernet

interface:iface_mac

interface:create
    ifacename
        $testnic

interface:iface_list
    flags
        all

interface:iface_list
    flags
        active

interface:iface_list
    flags
        inactive
